import React from 'react'

export default function AlphaValueInput({ opacityValue }) {
    return(
        <div className="color-value">
            <span>{ opacityValue }</span>
        </div>
    );
}